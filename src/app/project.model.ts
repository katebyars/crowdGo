export class Project {
  constructor(public name: string, public description: string, public author: string, public pledged: number, public funded: boolean, public daysToGo: number, public goal: number, public category: string, public image: string) { }
}
